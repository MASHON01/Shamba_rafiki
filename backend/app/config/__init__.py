"""
Application configuration: settings, paths, constants, logging.

This marker makes `app.config` an explicit package. Import the
concrete objects from their modules (e.g.
`from app.config.settings import settings`).
"""

from __future__ import annotations