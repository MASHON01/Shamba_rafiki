"""
Cross-cutting utilities: logging and caching helpers.

This marker makes `app.utils` an explicit package. Import helpers
from their modules (e.g. `from app.utils.logger import get_logger`).
"""

from __future__ import annotations