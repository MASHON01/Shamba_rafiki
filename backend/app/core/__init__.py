"""
Core infrastructure: exceptions, responses, lifecycle, registry,
dependency container, bootstrap.

The shared backbone every other package builds on. This marker makes
`app.core` an explicit package.
"""

from __future__ import annotations