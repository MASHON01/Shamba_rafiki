"""
Pydantic domain models: requests, responses, documents, language,
metadata, classifier.

This marker makes `app.models` an explicit package. Import concrete
models from their modules (e.g.
`from app.models.document import DocumentChunk`).
"""

from __future__ import annotations